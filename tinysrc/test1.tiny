int main() {
    float a;
    float b; 
    int ccc := 2;
    bool tmp;
    a := (10.44*356+1.28) / 2 + 1024 * 1.6;
    read ccc;
    b := a * ccc - a / ccc;
    tmp := a < b;

    write(a);
    write(b);
    write(ccc);
    write(tmp);
    write add(1, 2,);

    return 0;
}

int add(int a, int b,) {
    return a + b;
}