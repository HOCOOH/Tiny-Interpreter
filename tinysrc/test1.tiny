int main() {
    float a;
    float b; 
    int ccc := 2;
    bool tmp;
    a := (10.44*356+1.28) / 2 + 1024 * 1.6;
    b := a * ccc - a / ccc;
    tmp := a < b;
    a := add(1, 2.4,);

    write(a);
    write(b);
    write(ccc);
    write(tmp);

    return 0;
}

int add(float a, float b,) {
    return a + b;
}